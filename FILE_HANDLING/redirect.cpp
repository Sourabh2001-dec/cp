#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    string line;
    while (cin >> line)
    {
        // cin >> line;
        cout << line << "-";
    }
    return 0;
}
